# Copy to .env and fill in. Get this token from the Cloudflare Zero Trust
# dashboard when you create the tunnel (Networks > Tunnels > Create a
# tunnel > Docker) — see the README's Cloudflare Tunnel section.
CLOUDFLARE_TUNNEL_TOKEN=
