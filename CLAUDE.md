# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository State

This is a freshly initialized repository. At the time of writing it contains only a `README.md`. No language runtime, build system, test framework, or source code has been added yet.

When the project is bootstrapped, update this file with:
- The commands needed to install dependencies, build, lint, and run tests (including how to run a single test file/case)
- The high-level architecture (entry points, major layers, data flow)
- Any non-obvious conventions or constraints
